<h1>This is the clans page.</h1>

<ul>
  <% @clans.each do |clan| %>
    <li><strong><%= link_to clan.name, clan_path(clan) %>:-</strong><%= clan.quote %></li>
  <% end %>
</ul>

<br>
<h2>Add a clan.</h2>
<%= link_to "New Clan", new_clan_path %>
